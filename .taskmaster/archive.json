{
  "archived": [
    {
      "id": 1,
      "title": "Add TOML config file support",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 2,
      "title": "Add optional OS keychain support",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 3,
      "title": "Measure binary size impact",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 4,
      "title": "macOS code signing and notarization",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 5,
      "title": "Windows code signing with Azure Trusted Signing",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 7,
      "title": "GitHub Actions release workflow",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 9,
      "title": "Redesign interactive secret input: single-line default, --multi-line, --trim",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 10,
      "title": "Hash owner_key (IP address) for privacy",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 11,
      "title": "Investigate release binary size optimization",
      "status": "cancelled",
      "priority": "low"
    },
    {
      "id": 12,
      "title": "Ensure Settings page buttons follow design system",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 13,
      "title": "Ensure all page headings use the header class",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 14,
      "title": "Ensure all links use the link class",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 16,
      "title": "Dashboard page: bring in line with design system",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 17,
      "title": "Define .label heading class or migrate to .heading",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 18,
      "title": "Extract reusable Alert component",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 19,
      "title": "Fix iOS auto-zoom on input focus (font-size < 16px)",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 20,
      "title": "Increase base font size for mobile readability",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 21,
      "title": "Add safe-area-inset padding for notched iPhones",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 22,
      "title": "Verify touch-friendly file selection in FileDropZone",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 23,
      "title": "Enable server-side compression (gzip/brotli)",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 24,
      "title": "Exclude test pages from Tailwind content scanning",
      "status": "cancelled",
      "priority": "low"
    },
    {
      "id": 25,
      "title": "Investigate dropdown menu wider than username button",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 23,
      "title": "Add paste-a-link support for claiming secrets",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 29,
      "title": "Add offline state detection and messaging",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 52,
      "title": "Fix keychain config writes string instead of bool for use_keychain",
      "status": "done",
      "priority": "high"
    },
    {
      "id": 54,
      "title": "Get-Secret form: handle cross-instance links correctly",
      "status": "done",
      "priority": "medium"
    },
    {
      "id": 46,
      "title": "In-CLI update check and self-update against GitHub Releases",
      "description": "Right now an old secrt CLI talking to a new server fails in cryptic ways (e.g., the v0.15.0 AAD format break: a 0.14.x CLI uploads a wrapper the new server stores but the new client cannot unwrap). For a security-sensitive tool this is two problems at once: (1) users may be running a version with a known vulnerability without noticing, and (2) the server cannot evolve its protocol without leaving stale clients in a silently-broken state. Solve both by having the CLI check GitHub Releases on a cached cadence, surface a clear stderr banner when out of date, and provide a single-command upgrade path that re-verifies the SHA-256 checksum the release workflow already publishes. GitHub Releases are signed (Apple Developer ID + Azure Trusted Signing for the macOS/Windows artifacts) and public, so no new infrastructure is required and supply-chain integrity is preserved end-to-end. Scope: this task is the CLI update story only \u2014 Homebrew, apt/RPM, and other package managers are deliberately out of scope until there is concrete user demand. The optional installer subtask covers first-time setup; everything after that goes through `secrt update`.\n\nTracking: GitHub issue https://github.com/getsecrt/secrt/issues/2 (project: https://github.com/orgs/getsecrt/projects/1).",
      "status": "done",
      "priority": "high",
      "dependencies": [],
      "subtasks": [
        {
          "id": 1,
          "title": "Spec: normative update-check + self-update behavior in spec/v1/cli.md",
          "description": "Before code: write a new section in spec/v1/cli.md (likely under HTTP Client Behavior) that defines the contract. Decisions to settle now, in writing: (a) Where does the latest version come from \u2014 direct GitHub API (api.github.com/repos/getsecrt/secrt/releases/latest filtered to cli/v* tags) or a server-provided field on /api/v1/info? Recommend GitHub direct because it is independent of any single secrt instance, cacheable via standard HTTP, and survives a server outage. (b) What is the cache cadence? 24h is a reasonable default, longer is wasteful, shorter risks GitHub rate-limit on heavy users. (c) Where is the cache stored? Recommend ~/.cache/secrt/update-check.json (XDG cache home), separate from the config dir so config-only deletion does not nuke the cache and vice versa. (d) When does the check fire? Once per command at most, only after the command's primary work completes (so a slow GitHub API call does not block secrt send), and never when stdin/stdout is being used for binary data. (e) What is the opt-out? --no-update-check flag and update_check = false config key, plus an environment variable SECRT_NO_UPDATE_CHECK=1 for CI/scripted contexts. (f) What output goes where? The banner is one dim line on STDERR \u2014 never stdout, never multi-line, never colorized when stderr is not a TTY. Suggested wording: secrt 0.16.0 is available (you have 0.15.0). Run 'secrt update' to upgrade. Audit before merge: must not break the existing 'pipe-friendly stdout' invariant the CLI spec already commits to.",
          "status": "pending"
        },
        {
          "id": 2,
          "title": "Implement the cached version check (read path)",
          "description": "Add a small module (suggested: crates/secrt-cli/src/update_check.rs) that exposes one entry point: pub fn check(deps: &Deps) -> Option<UpdateAvailable>. Behavior: read ~/.cache/secrt/update-check.json (or the platform-equivalent cache dir). If the cached checked_at is within the configured interval (default 24h), use the cached latest_version. Otherwise issue a single GET to https://api.github.com/repos/getsecrt/secrt/releases?per_page=30 with a 5s timeout, filter for tags matching cli/v\\d+\\.\\d+\\.\\d+ (skip drafts and prereleases), pick the highest semver, and write the result back to the cache file. On any error (timeout, parse error, IO error) return None and silently move on \u2014 an update check must never break a working CLI invocation. Use the existing ureq client to avoid adding a new HTTP dep. Return Some(UpdateAvailable { latest, current }) only if latest > current. Test coverage: inject a stub HTTP fn via the existing Deps pattern, table-test the cache hit/miss paths, verify that a 503 from GitHub is silent.",
          "status": "pending"
        },
        {
          "id": 3,
          "title": "Wire the banner into the command dispatcher",
          "description": "After the primary command's output has been written and just before exit, call update_check::check() and emit the banner if Some(_) is returned. Wiring rules: (a) The banner is suppressed when --silent, --no-update-check, update_check = false in config, or SECRT_NO_UPDATE_CHECK=1 in env. (b) The banner is also suppressed when --json is set (machine-readable mode must stay clean). (c) The banner is suppressed when the current command is secrt update itself (avoid double-talking). (d) If stderr is not a TTY, drop ANSI color codes but still emit the line \u2014 pipelines that grep stderr for warnings should still work. Add an e2e-tier test that sets SECRT_NO_UPDATE_CHECK=1 and verifies stderr is exactly the expected output (no banner). Write a separate test that points the check fn at a fake 'newer version available' result and verifies the banner appears once and only once.",
          "status": "pending"
        },
        {
          "id": 4,
          "title": "Implement `secrt update` (self-update) subcommand",
          "description": "Adds a top-level command that downloads the right release artifact for the current OS+arch, verifies the SHA-256 against secrt-checksums-sha256.txt from the same release, and replaces the running binary atomically. OS+arch detection: cargo-style (std::env::consts::OS / ARCH) -> asset name mapping (secrt-linux-amd64.tar.gz, secrt-linux-arm64.tar.gz, secrt-macos-arm64.pkg or the universal secrt-macos.pkg, secrt-windows-amd64.zip, etc.). On macOS, decide whether to handle the .pkg directly (preferred \u2014 keeps notarization intact) or fall back to extracting a tarball; check what artifacts the release-cli workflow currently publishes per platform. Atomic replace strategy: download to a temp file in the same directory as the running binary (so the rename does not cross filesystems), chmod 755, then rename(2) over the running file. On Unix this works because the kernel keeps the inode of the running process alive after rename. On Windows it does not \u2014 the binary file is locked while running \u2014 so the implementation needs to write to secrt.exe.new, schedule a MoveFileEx(MOVEFILE_DELAY_UNTIL_REBOOT) or use a self-restart shim. Document the Windows quirk in the help text: 'on Windows, the new version takes effect after the next launch.' Flags: --check (just print whether an update is available, no download), --force (download and reinstall even if already on latest), --version <X.Y.Z> (pin to a specific version, useful for downgrades during incident response). Always re-verify the SHA-256, even with --force. Never trust the existing local binary as a check against the new one \u2014 verify against the published checksum file every time. The gh CLI's own gh extension upgrade is a useful reference implementation.",
          "status": "pending"
        },
        {
          "id": 5,
          "title": "Server-side min_supported_cli_version field on /api/v1/info (optional but recommended)",
          "description": "Add an optional min_supported_cli_version field to the /api/v1/info response. The CLI checks this on every server request that hits /api/v1/info (which secrt auth status and similar already do) and prints a hard 'this CLI is too old to talk to this server safely \u2014 please upgrade' message before continuing. This complements the GitHub-Releases-based check by giving the server an authoritative way to declare a hard floor \u2014 useful for situations like the v0.15.0 AAD break where some commands are silently broken on old clients. The field is advisory only: the server does not refuse old clients (that would break the secrt get zero-knowledge bearer flow for anyone who got a link before they upgraded). The CLI uses the field to inform the user, not to block them. Spec change in spec/v1/api.md and spec/v1/server.md; default behavior when the field is absent is to fall back to the GitHub-Releases check.",
          "status": "pending"
        },
        {
          "id": 6,
          "title": "User-friendly error reporting for failed self-updates",
          "description": "When secrt update fails (network error, SHA-256 mismatch, write-protected install path, /usr/local/bin not writable without sudo, etc.), the error message must tell the user exactly what happened and what to try next: (a) network errors -> suggest --proxy env vars, retry, or manual download from the printed release URL; (b) SHA-256 mismatch -> halt immediately, do not install, surface both expected and actual hashes (this is a security-sensitive failure and must be loud); (c) permission-denied on the install path -> suggest re-running with sudo secrt update or specifying --install-dir <path>; (d) Windows file-lock -> fall back to the deferred-rename path automatically, tell the user the upgrade lands at next launch. Log nothing remotely \u2014 this is a privacy tool. The user is the only audience for these errors.",
          "status": "pending"
        },
        {
          "id": 7,
          "title": "First-time installer script at secrt.ca/install.sh (optional)",
          "description": "Companion to the in-CLI updater for first-time users who do not have the binary yet. A single bash script that detects OS+arch, pulls the matching artifact from the latest GitHub release, verifies the published SHA-256, and installs to /usr/local/bin/secrt (or ~/bin/secrt if /usr/local is not writable). Usage pattern is the standard pipe-to-shell one popularized by Rustup, Bun, and similar tools. Mitigations for the well-known reputational issue: (a) source-mirror the script in the repo so it can be audited by URL: visit github.com/getsecrt/secrt/blob/main/scripts/install.sh to read the same code that secrt.ca/install.sh serves; (b) print the URL prominently in the install script so users who run it once can re-run from the local file; (c) document the audit-first path in the README \u2014 fetch the script with -o install.sh, inspect with less, then run with bash. Once installed, secrt update is the maintenance path \u2014 install.sh is one-and-done.",
          "status": "pending"
        },
        {
          "id": 8,
          "title": "Tests + spec vectors for the update flow",
          "description": "Coverage targets: (a) update-check module: cache hit, cache miss, network error, GitHub rate-limit (403), invalid JSON, semver comparison edge cases (0.14.10 vs 0.15.0, 0.15.0 vs 0.15.0-rc.1), tag-prefix mismatch, drafts and prereleases skipped. (b) Banner suppression matrix: --silent, --no-update-check, SECRT_NO_UPDATE_CHECK=1, --json, current command is secrt update, stderr is not a TTY. Each as its own table-driven test row. (c) Self-update: SHA-256 mismatch refuses installation, atomic rename works on Unix, the Windows path tries the deferred-rename fallback (gate behind cfg(windows)), --version pinning resolves to the exact release tag. (d) Optional: a CI job that builds a known-good 0.15.0-test artifact, runs secrt update --version 0.15.0-test --install-dir /tmp/test-bin, and asserts the installed binary's --version output matches. Real GitHub API hits should be gated behind the same e2e ignore pattern the existing CLI tests use.",
          "status": "pending"
        }
      ]
    },
    {
      "id": 51,
      "title": "Fix invalid docs URL in CLI config.toml template",
      "description": "The CLI's config.toml template references https://secrt.ca/docs/config which doesn't resolve. Either point at the README section that documents config, or stand up a real /docs/config page. Lower-stakes fix; visible to every user who runs `secrt config init`. Tracking: GitHub issue https://github.com/getsecrt/secrt/issues/41.",
      "status": "done",
      "priority": "low",
      "dependencies": [],
      "subtasks": [
        {
          "id": 1,
          "title": "Decide: README anchor or new docs page",
          "description": "Cheapest is to point at https://github.com/getsecrt/secrt/blob/main/crates/secrt-cli/README.md#configuration (or wherever the CLI README documents config keys). Alternative: create a docs page on secrt.ca and route to it. Decision drives which fix to apply.",
          "status": "pending"
        },
        {
          "id": 2,
          "title": "Patch the template in crates/secrt-cli/src/config.rs",
          "description": "The CONFIG_TEMPLATE constant in config.rs is the source. Update the URL there. Add a tiny test that asserts the template references a non-empty plausible URL.",
          "status": "pending"
        }
      ]
    },
    {
      "id": 55,
      "title": "secrt update: suggest sudo when system-wide install is detected",
      "description": "When `secrt update` hits a permission-denied error and the binary lives in a known system path (e.g. /usr/local/bin, /usr/bin, /opt/*/bin), the error message should mention `sudo secrt update` alongside the existing `--install-dir` suggestion. The 0.16.0 design (`crates/secrt-cli/src/update.rs:1007,1022`) deliberately omitted sudo because the assumed install location was user-space \u2014 but for a deliberately root-owned system install, `--install-dir ~/.local/bin` is actively counterproductive: it leaves the old vulnerable binary at /usr/local/bin and creates a parallel install whose precedence depends on PATH ordering. JD hit this on his dev machine running 0.16.1 (binary at /usr/local/bin/secrt, root-owned). Keep `--install-dir` listed first in the message so the least-privilege default still nudges new installs into user-space; add the sudo hint as a second option only when the heuristic confirms a system install.",
      "status": "done",
      "priority": "low",
      "dependencies": [],
      "subtasks": [
        {
          "id": 1,
          "title": "Extract message-formatting into a pure function",
          "description": "Refactor the duplicated permission-denied error string at update.rs:1007 and update.rs:1022 into a single helper, e.g. `fn permission_denied_message(install_dir: &Path, release_url: &str) -> String`. Both call sites then become `writeln!(deps.stderr, \"{}\", permission_denied_message(...))`. Pure function \u2014 no IO, fully unit-testable.",
          "status": "done"
        },
        {
          "id": 2,
          "title": "Add system-install heuristic",
          "description": "Add `fn looks_like_system_install(install_dir: &Path) -> bool`. True when `install_dir` is one of: `/usr/local/bin`, `/usr/bin`, `/usr/local/sbin`, `/usr/sbin`, or matches `/opt/*/bin`. False otherwise (including ~/ anything, /tmp, cargo bin, brew Cellar, etc \u2014 those are either caught by managed-install classification earlier or genuinely user-space). Unix-only behavior; on Windows the heuristic is always false (no privilege-elevation hint there \u2014 Windows installs are typically per-user or via installer/MSI).",
          "status": "done"
        },
        {
          "id": 3,
          "title": "Wire heuristic into the message",
          "description": "When `looks_like_system_install(install_dir)` is true, the message gains a third line / inline alternative: `or, if you installed system-wide: sudo secrt update`. Keep `--install-dir` first to preserve the user-space default. Manual download stays as fallback. Phrasing must avoid making sudo look like the recommended path.",
          "status": "done"
        },
        {
          "id": 4,
          "title": "Tests",
          "description": "Two test groups, both pure-function (no IO, no root): (a) `looks_like_system_install` table-driven across `/usr/local/bin`, `/usr/bin`, `/opt/secrt/bin`, `~/.local/bin`, `~/.cargo/bin`, `/tmp/foo`, on Windows always false. (b) `permission_denied_message` with two cases: user-writable path (current message, no sudo), and system path (message includes the `sudo secrt update` line and still mentions `--install-dir`). Co-locate with the existing update.rs tests near line 1345.",
          "status": "done"
        },
        {
          "id": 5,
          "title": "Spec touch-up (optional)",
          "description": "If `spec/v1/cli.md` documents the permission-denied behavior verbatim (it might, given the 0.16.0 update spec section), update the spec to note the new conditional sudo hint. If the spec only documents exit codes, no change needed.",
          "status": "done"
        }
      ]
    },
    {
      "id": 56,
      "title": "Expose server version on /api/v1/info and X-Secrt-Server-Version response header",
      "description": "/api/v1/info currently broadcasts CLI version info (latest_cli_version, latest_cli_version_checked_at, min_supported_cli_version) but says nothing about the server itself. Add a server_version field and a matching X-Secrt-Server-Version response header so operators can verify deploys without SSH, and so the CLI can record which server version a response came from. Source the value from env!(\"CARGO_PKG_VERSION\") at compile time (same way the CLI surfaces its own version).",
      "status": "done",
      "priority": "low",
      "dependencies": [],
      "subtasks": [
        {
          "id": 1,
          "title": "Add server_version to InfoResponse and populate on the server",
          "description": "Add `pub server_version: Option<String>` to `InfoResponse` in `crates/secrt-core/src/api.rs` (Optional + #[serde(default)] so older CLIs deserialize new responses cleanly, mirroring how latest_cli_version was added in 0.16.0). On the server side, populate it from `env!(\"CARGO_PKG_VERSION\")` in the /info handler. Use `Option<String>` rather than `String` so the field shape matches the existing CLI-version trio and #[serde(default)] alone covers older clients.",
          "status": "done"
        },
        {
          "id": 2,
          "title": "Add X-Secrt-Server-Version response header on every response",
          "description": "Mirror the existing X-Secrt-Latest-Cli-Version family. Add a middleware-applied response header on every authenticated and public response (including /healthz). Set value from env!(\"CARGO_PKG_VERSION\"). The CLI can then opportunistically record server version from any response without round-tripping /info. Update `crates/secrt-server/src/http/` middleware that already adds the cli-version headers \u2014 single new line per header.",
          "status": "done"
        },
        {
          "id": 3,
          "title": "Spec + OpenAPI",
          "description": "Update spec/v1/api.md to document `server_version` in the InfoResponse section and `X-Secrt-Server-Version` in the headers section. Update spec/v1/openapi.yaml to include the new field (Optional<String>) and the new response header. Match the prose style used for the latest_cli_version trio.",
          "status": "done"
        },
        {
          "id": 4,
          "title": "Tests",
          "description": "(a) secrt-core: deserialization test confirming InfoResponse parses both with and without server_version (#[serde(default)] semantics \u2014 like the existing info_response_parses_without_cli_version_fields test). (b) secrt-server: integration test asserting /api/v1/info returns server_version == env!(\"CARGO_PKG_VERSION\") and that X-Secrt-Server-Version is on the response of an arbitrary endpoint (e.g. /healthz). Both pure tests \u2014 no live server, no DB.",
          "status": "done"
        },
        {
          "id": 5,
          "title": "CLI consumption (deferred decision)",
          "description": "Out of scope for this task: how/whether the CLI surfaces the discovered server version (e.g. in error messages, in `secrt --version` after a successful API call, or in a future min-supported-server-version compatibility check). Implementation choices range from \"silently record in cache for diagnostics\" to \"include in error footers\" to \"compatibility gate\". Punt to a follow-up task once we have a concrete use case; for now the field is exposed and the CLI can adopt at its own pace."
        }
      ]
    },
    {
      "id": 57,
      "title": "Flip CSP from Report-Only to enforcing (and add Trusted Types)",
      "description": "0.16.4 shipped a strict Content-Security-Policy as Report-Only on HTML responses (see crates/secrt-server/src/http/security.rs). Cross-browser soak (Chrome / Safari / Firefox desktop, full app walk on 2026-04-27) found zero real violations \u2014 the strict policy is shippable as-written, no widenings needed. After ~1 week of prod soak passes without new reports, flip the header from content-security-policy-report-only to content-security-policy, fold upgrade-insecure-requests back into the main policy, and drop the separate enforcing header. Same release: add require-trusted-types-for 'script'; trusted-types 'none' as Report-Only (web/src/main.tsx already migrated to replaceChildren() to make this safe). A second follow-up release flips Trusted Types to enforcing. Caddy-side CSP has already been removed on both secrt.is and secrt.ca (2026-04-27); deployment guide updated. Ship as a small patch release.",
      "status": "done",
      "priority": "medium",
      "dependencies": [],
      "subtasks": []
    },
    {
      "id": 61,
      "title": "WebAuthn passkey signature verification (shipped 0.17.0)",
      "description": "Real ECDSA-P256 / ES256 verification on all passkey /finish handlers, replacing the challenge-id bearer model. Verifier in crates/secrt-server/src/domain/webauthn.rs is a pure-function module covered by spec/v1/webauthn.vectors.json (9 vectors, generated by an independent Python ref so passing them is the actual correctness signal). Wire shape for register/add finish now carries authenticator_data + client_data_json (drops public_key); login finish adds signature. Greenfield wire break \u2014 no MIN_SUPPORTED_CLI bump (CLI doesn't talk to passkey endpoints). Web client uses response.getAuthenticatorData() (Chrome 85+/Firefox 119+/Safari 17.4+). Plan handoff at ~/.claude/plans/webauthn-sig-verify.md. Deployed and reset on secrt.is + secrt.ca on 2026-04-28.",
      "status": "done",
      "priority": "high",
      "dependencies": [],
      "subtasks": [
        {
          "id": 1,
          "title": "Hand-roll vs webauthn-rs decision documented",
          "status": "done",
          "description": "Hand-roll with ring only \u2014 no ciborium dep. Rationale recorded in plan."
        },
        {
          "id": 2,
          "title": "TestKeyPair + TestPasskey helpers",
          "status": "done",
          "description": "P-256 keypair, COSE_Key emitter, ECDSA-DER signer in tests/helpers/webauthn.rs. TestPasskey drives full /start\u2192/finish round-trips; tests pass real WebAuthn ceremonies through the router."
        },
        {
          "id": 3,
          "title": "Independent test vectors (Python pyca/cryptography)",
          "status": "done",
          "description": "scripts/generate_webauthn_vectors.py builds CBOR/authData/cdj from spec text using pyca crypto for the ECDSA primitive. spec/v1/webauthn.vectors.json \u2014 1 happy register, 1 happy login, 7 negative cases. Verifier passes all on first run."
        },
        {
          "id": 4,
          "title": "domain/webauthn.rs verifier",
          "status": "done"
        },
        {
          "id": 5,
          "title": "Wire types + plumb into 3 /finish handlers",
          "status": "done"
        },
        {
          "id": 6,
          "title": "Spec rewrite (server.md \u00a76.2, api.md, openapi.yaml)",
          "status": "done"
        },
        {
          "id": 7,
          "title": "Web client forwards assertion fields",
          "status": "done",
          "description": "createPasskeyCredential uses response.getAuthenticatorData(); getPasskeyCredential surfaces signature. LoginPage does two navigator.credentials.get() calls (picker + server-challenge re-assertion)."
        },
        {
          "id": 8,
          "title": "secrt-admin reset subcommand",
          "status": "done",
          "description": "Interactive typed-confirmation guard. Truncates 10 data tables. Used during the 0.17.0 cutover."
        },
        {
          "id": 9,
          "title": "Single-prompt login UX",
          "status": "done",
          "description": "Shipped in 0.17.1 \u2014 discoverable-credential flow. /login/start makes credential_id optional; client does a single navigator.credentials.get() against the server's challenge. The /finish credential binding now comes solely from the signature. Triggered by iCloud Passwords' policy of prompting for the OS account password on every get() call."
        },
        {
          "id": 10,
          "title": "Wildcard subdomain origin allow-list",
          "status": "pending",
          "description": "Origin check is a strict string equality against public_base_url. Wildcard subdomains (my.secrt.is, foo.secrt.ca) will fail with OriginMismatch once they're hit by a real user. Fix: make the verifier accept a predicate or allow-list, or have the handler validate origin against rp_id-suffix before passing to the verifier. Documented in spec \u00a76.2."
        },
        {
          "id": 11,
          "title": "RS256 algorithm support (deferred)",
          "status": "deferred",
          "description": "Only ES256 / EC2-P256 accepted at MVP. RS256 is a Windows-Hello-specific niche; real-world coverage is essentially complete with ES256. Re-evaluate when a real user reports a failed registration."
        }
      ]
    }
  ]
}
