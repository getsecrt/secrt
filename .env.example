# Server
ENV=development
LISTEN_ADDR=:8080
PUBLIC_BASE_URL=https://secrt.ca
LOG_LEVEL=info

# Database (Postgres)
# Configure either:
# - `DATABASE_URL` (recommended for production/managed DBs; many platforms provide this), OR
# - the individual `DB_*` fields (often easiest for local dev). The app will build a URL for you.
DATABASE_URL=

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=secret
DB_USER=secret_app
DB_PASSWORD=
# SSL/TLS:
# - Local dev (same host): disable
# - Production / remote DB: require (or verify-full + DB_SSLROOTCERT for strongest)
DB_SSLMODE=disable
# Optional (managed DBs / SSL verification)
DB_SSLROOTCERT=

# Auth
# Used as a "pepper" for hashing API keys before storing them.
# Generate with: openssl rand -base64 32
# REQUIRED in production (server refuses to start without it).
API_KEY_PEPPER=

# Per-tier envelope size limits (bytes). Controls max size of a single secret.
# PUBLIC_MAX_ENVELOPE_BYTES=262144    # 256 KB (default)
# AUTHED_MAX_ENVELOPE_BYTES=1048576   # 1 MB (default)

# Per-owner storage quotas.
# PUBLIC_MAX_SECRETS=10               # max active secrets per IP (default)
# PUBLIC_MAX_TOTAL_BYTES=2097152      # 2 MB total per IP (default)
# AUTHED_MAX_SECRETS=1000             # max active secrets per API key (default)
# AUTHED_MAX_TOTAL_BYTES=20971520     # 20 MB total per API key (default)

# IMPORTANT: This .env file is for LOCAL DEVELOPMENT ONLY.
# In production, use systemd EnvironmentFile= with restricted permissions.
# See docs/credentials-and-deployment.md for details.
